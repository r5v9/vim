
To install, use:

<pre>
git clone git://github.com/rufiao/vim.git .vim && cd .vim && git submodule init && git submodule update && cd bundle/command-t && rake make && cd ../../.. && ln -s .vim/vimrc .vimrc
</pre>

To have python support in syntastic:

<pre>
sudo pip install pyflakes
</pre>

